# Development Instructions
* Currently, there are dependency conflicts! To install, use `npm ci --legacy-peer-deps`.
* To run web version locally, use `npm run dev`.
* To export web version to `dist` folder, use `npm run build`.